/**
 * export const tabTypes = {
 *  basic: Symbol(),
 *  super: Symbol()
 * }
 *
 * use case:
 * export default defineComponent({
 *  const change = (type) => {
 *      if (type === tabTypes.basic) {
 *          // render basic content
 *      }
 *      if (type === tabTypes.super) {
 *          // render super content
 *      }
 *  }
 *  reutrn { tabTypes, change }
 * })
 */
